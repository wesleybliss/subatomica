/*import { createInsertSchema, createSelectSchema } from 'drizzle-orm'
import { teams } from '@repo/api/db/schema'

// Insert schema (excludes non-null defaults like updatedAt)
export const insertSchema = createInsertSchema(yourTable)

// Select schema (includes all columns as they'll appear from DB)
export const selectSchema = createSelectSchema(yourTable)

// Update schema (partial updates)
export const updateSchema = createUpdateSchema(yourTable)

export type Team = z.infer<typeof TeamSchema>*/

export default 'todo'
